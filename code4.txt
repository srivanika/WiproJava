import java.io.*;
import java.util.*;

// Read only region start
class UserMainCode
{
    public int lastDigitOf(int input1)
    {
        // Read only region end

        input1 = Math.abs(input1);

        return input1 % 10;
    }
}